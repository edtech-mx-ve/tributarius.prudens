# Aceptación Sprint 19I.15

- carga validada del sidecar temporal;
- LIVA y CPEUM quedan bloqueados por gap documental fail-closed;
- un documento sin gap no se bloquea;
- el puente RAG consulta el guard antes de crear candidato normativo;
- el runtime puede exigir el registro mediante configuración;
- no se asignan `effective_from`/`effective_to`;
- no se modifica corpus, chunks, embeddings ni FAISS.
