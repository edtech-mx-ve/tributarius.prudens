# Aceptación Sprint 19I.18G

- los 14 candidatos normativos aparecen en el runtime;
- no hay discrepancias de `source_type` o `source_role`, salvo reporte explícito;
- la identidad de fuente es auditable mediante `source_sha256`;
- mismatches de hash se reportan de forma fail-closed;
- marcadores editoriales se envían a revisión manual;
- ningún documento se promueve automáticamente a publicable;
- se genera reporte local bajo `reports/sprint19I18G/`;
- Ruff, mypy y pytest completos permanecen limpios.
