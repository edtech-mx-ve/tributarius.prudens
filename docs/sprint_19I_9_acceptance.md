# Aceptación Sprint 19I.9

Criterios:

- `/ready` devuelve HTTP 200;
- `/health` devuelve HTTP 200;
- `/` devuelve HTTP 200;
- las tres consultas devuelven `status=ready`;
- LIVA aparece en evidencia para el smoke LIVA;
- CPEUM aparece en evidencia para el smoke constitucional;
- LIEPS aparece en evidencia para el smoke IEPS;
- no se modifica `rag_artifact_dir` por defecto todavía;
- no se toca GitHub ni Render.
